<x-table.cell>
    <a href="{{ $url }}"
        class="flex justify-center text-{{ $color }}-400 dark:hover:bg-{{ $color }}-400 dark:hover:text-white">
        {{ __($text) }}
    </a>
</x-table.cell>
